---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - telegram-bot-i18n.js
  - telegram-bot.js
autonomous: true
requirements:
  - ARCH-01

must_haves:
  truths:
    - "telegram-bot-i18n.js exists as a standalone CommonJS module exporting the BOT_I18N object"
    - "telegram-bot.js imports BOT_I18N from telegram-bot-i18n.js via require()"
    - "telegram-bot.js is ~825 lines shorter (lines 42-825 removed, replaced by one require line)"
    - "The _t() method on TelegramBot class continues to work identically (no signature change)"
    - "All three locale dictionaries (uk, en, ru) are present in the extracted file with the same key counts"
  artifacts:
    - path: "telegram-bot-i18n.js"
      provides: "BOT_I18N object with uk, en, ru locale dictionaries"
      contains: "module.exports"
    - path: "telegram-bot.js"
      provides: "TelegramBot class importing i18n from separate file"
      contains: "require('./telegram-bot-i18n')"
  key_links:
    - from: "telegram-bot.js"
      to: "telegram-bot-i18n.js"
      via: "require('./telegram-bot-i18n')"
      pattern: "const BOT_I18N = require\\('./telegram-bot-i18n'\\)"
---

<objective>
Extract the BOT_I18N translation object (lines 42-825 of telegram-bot.js) into a separate `telegram-bot-i18n.js` file.

Purpose: Reduces telegram-bot.js by ~783 lines of pure data, making FSM migration diffs in Plan 02 cleaner and easier to review. This is the ARCH-01 requirement.

Output: `telegram-bot-i18n.js` (new file, ~825 lines) and a slimmer `telegram-bot.js` that imports it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create telegram-bot-i18n.js with BOT_I18N export</name>
  <files>telegram-bot-i18n.js</files>
  <read_first>
    - telegram-bot.js (lines 1-826 -- the BOT_I18N object and its boundaries)
  </read_first>
  <action>
    Create a new file `telegram-bot-i18n.js` in the project root (same directory as `telegram-bot.js`).

    The file structure must be:
    ```javascript
    // telegram-bot-i18n.js -- Internationalization data for Telegram Bot
    // Extracted from telegram-bot.js to reduce main file size.
    'use strict';

    const BOT_I18N = {
      // ... exact contents from telegram-bot.js lines 43-824 ...
    };

    module.exports = BOT_I18N;
    ```

    Steps:
    1. Read `telegram-bot.js` lines 42-825 (the entire `const BOT_I18N = { ... };` block)
    2. Create `telegram-bot-i18n.js` with `'use strict';` header, the BOT_I18N object (copied verbatim), and `module.exports = BOT_I18N;` at the end
    3. The object has three top-level keys: `uk`, `en`, `ru` -- each containing ~273 translation keys
    4. Do NOT modify any translation strings, key names, or structure -- this is a pure mechanical move
    5. Do NOT change the variable name from `BOT_I18N`
    6. The comment header at line 40 (`// --- Bot Internationalization ---`) should be included in the new file as context
  </action>
  <verify>
    <automated>node -e "const i = require('./telegram-bot-i18n'); const uk = Object.keys(i.uk).length; const en = Object.keys(i.en).length; const ru = Object.keys(i.ru).length; console.assert(uk > 250, 'uk keys: ' + uk); console.assert(en > 250, 'en keys: ' + en); console.assert(ru > 250, 'ru keys: ' + ru); console.assert(typeof i.uk.rate_limit === 'string'); console.assert(typeof i.en.rate_limit === 'string'); console.assert(typeof i.ru.rate_limit === 'string'); console.log('OK: uk=' + uk + ' en=' + en + ' ru=' + ru);"</automated>
  </verify>
  <acceptance_criteria>
    - `telegram-bot-i18n.js` exists in project root
    - File starts with `'use strict';`
    - File contains `const BOT_I18N = {`
    - File ends with `module.exports = BOT_I18N;`
    - `node -e "require('./telegram-bot-i18n')"` exits with code 0
    - `Object.keys(require('./telegram-bot-i18n').uk).length` is > 250
    - `Object.keys(require('./telegram-bot-i18n').en).length` is > 250
    - `Object.keys(require('./telegram-bot-i18n').ru).length` is > 250
    - First key in uk dictionary is `rate_limit` (verifies exact content preserved)
  </acceptance_criteria>
  <done>telegram-bot-i18n.js loads via require() and contains all three locale dictionaries with correct key counts</done>
</task>

<task type="auto">
  <name>Task 2: Replace BOT_I18N inline definition with require() import in telegram-bot.js</name>
  <files>telegram-bot.js</files>
  <read_first>
    - telegram-bot.js (lines 1-50 to see current imports and BOT_I18N start)
    - telegram-bot.js (lines 820-870 to see BOT_I18N end, class definition start, and _t() method)
    - telegram-bot-i18n.js (the newly created file from Task 1 -- verify it exists)
  </read_first>
  <action>
    Modify `telegram-bot.js` to import BOT_I18N from the extracted file instead of defining it inline.

    Specific changes:
    1. Replace lines 40-825 (the comment `// --- Bot Internationalization ---` through the closing `};`) with a single require line:
       ```javascript
       // --- Bot Internationalization ---
       const BOT_I18N = require('./telegram-bot-i18n');
       ```
    2. Do NOT modify the `_t()` method (lines 861-868 in the original file). It reads `BOT_I18N[this.lang]` which will work identically since the variable name and shape are preserved.
    3. Do NOT modify the `class TelegramBot extends EventEmitter {` line or anything after it.
    4. Do NOT modify the lines BEFORE line 40 (the constants, require statements, and pattern arrays).
    5. The file should go from ~4693 lines to ~3870 lines (a reduction of ~823 lines).

    After editing, verify with `node -c telegram-bot.js` (syntax check).
  </action>
  <verify>
    <automated>node -c telegram-bot.js && node -e "const i = require('./telegram-bot-i18n'); console.assert(Object.keys(i.uk).length > 250); console.log('syntax OK, i18n loads OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `telegram-bot.js` contains exactly one line matching `require('./telegram-bot-i18n')`
    - `telegram-bot.js` does NOT contain the string `'rate_limit': '` (the old inline uk dictionary first entry)
    - `telegram-bot.js` still contains `class TelegramBot extends EventEmitter`
    - `telegram-bot.js` still contains `_t(key, params = {})` method unchanged
    - `telegram-bot.js` still contains `module.exports = TelegramBot;` at the end
    - `node -c telegram-bot.js` exits 0 (no syntax errors)
    - Line count of telegram-bot.js is between 3800 and 3950 (was 4693, removed ~783 lines of i18n data, added 1 require line)
  </acceptance_criteria>
  <done>telegram-bot.js imports BOT_I18N via require(), the inline object is removed, syntax is valid, and line count is ~3870</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `node -c telegram-bot.js` -- syntax check passes
2. `node -e "require('./telegram-bot-i18n')"` -- i18n module loads
3. `node -e "const i = require('./telegram-bot-i18n'); console.assert(Object.keys(i.uk).length > 250 && Object.keys(i.en).length > 250 && Object.keys(i.ru).length > 250); console.log('All locales OK');"` -- all locales present
4. `grep -c "require('./telegram-bot-i18n')" telegram-bot.js` returns exactly `1`
5. `grep -c "pendingInput" telegram-bot.js` returns the SAME count as before (i18n extraction does not touch state fields)
</verification>

<success_criteria>
- telegram-bot-i18n.js exists with all 3 locale dictionaries (uk, en, ru) each having 250+ keys
- telegram-bot.js is ~823 lines shorter and imports BOT_I18N via require
- The _t() method signature and behavior are unchanged
- Both files pass Node.js syntax validation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
