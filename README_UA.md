# Claude Code Studio

**Повноцінний веб-воркспейс для [Claude Code](https://claude.ai/code)** — чат, Kanban дошка задач, мульти-агентна оркестрація, MCP сервери, Skills, проекти — все в одному місці. Без кроку збірки.

> Мови: [English](README.md) | [Українська](README_UA.md) | [Русский](README_RU.md)

---

## Можливості

| Функція | Опис |
|---------|------|
| 🖥 CLI Mode | Через `claude` CLI з Max підпискою (без витрат на API) |
| 💬 Чат у реальному часі | WebSocket стрімінг з рендерингом markdown |
| 👥 Мульти-Агент | Оркестрація команди агентів з графом залежностей |
| ⚡ MCP Сервери | Підключення будь-яких MCP серверів — пресети + кастомні |
| 🧠 Skills | Завантаження `.md` skill файлів у системний промпт Claude |
| 🔄 Режими | Auto / Planning / Task режими виконання |
| 💎 Моделі | Opus 4.6 / Sonnet 4.6 / Haiku 4.5 |
| 📁 Файловий браузер | Перегляд workspace, прев'ю файлів, прикріплення через `@mention` |
| 🖼 Vision | Вставка зображень з буфера обміну як vision блоків |
| 📋 Історія | Постійні сесії в SQLite з можливістю відновлення |
| ⚙️ Config Editor | Редагування `config.json`, `CLAUDE.md`, `.env` прямо в UI |
| 🔒 Auth | bcrypt пароль + 30-денні сесійні токени |
| 🐳 Docker | Dockerfile + docker-compose в комплекті |
| 🗂 Проекти | Іменовані проекти з власним workdir, git init, браузер директорій |
| 🪟 Паралельні вкладки | Кілька вкладок незалежно; стрімінг зберігається при перемиканні |
| 📊 Статистика | Денне/тижневе використання Claude Max, оцінка контекстних токенів |
| 🔐 Безпека мульти-екземплярів | File-lock хуки; workdir-черга в Kanban |
| 📋 Kanban дошка | Drag-and-drop дошка на `/kanban` — задачі запускають Claude Code автоматично |
| 📎 Вкладення файлів | Прикріплення зображень і текстових файлів до карток Kanban |
| 🔁 Retry | Авто-перезапуск задачі після краша з лічильником; без дублювання повідомлень |
| 🌍 i18n | Інтерфейс на Українській / Англійській / Російській (автовизначення) |

---

## Скріншоти

### Екран авторизації
Безпечна автентифікація з bcrypt-хешуванням паролів та 30-денними сесійними токенами.

![Екран авторизації](public/screenshots/01-login.png)

### Інтерфейс чату
Повноцінний воркспейс: чат у реальному часі через WebSocket з рендерингом markdown, перемикачі MCP серверів, 20+ пресетів навичок (автопідбір по задачі), файловий браузер з прев'ю, вибір моделі та режиму — все в одному dark-themed SPA.

![Інтерфейс чату](public/screenshots/02-main-chat.png)

### Kanban дошка
Drag-and-drop дошка задач з автоматичним запуском Claude Code. Перемістіть картку в "До виконання" — Claude бере задачу, виконує її та звітує. Авто-retry при краші, фільтрація по проектах, вибір моделі на кожну картку.

![Kanban дошка](public/screenshots/03-kanban.png)

---

## Встановлення та запуск

### Спосіб 1 — Запуск через npx (без встановлення)

Найпростіший спосіб. Завантажує та запускає останній реліз одразу:

```bash
npx github:Lexus2016/claude-code-studio
# Відкрийте http://localhost:3000
```

Або встановіть глобально та запускайте будь-коли:

```bash
npm install -g github:Lexus2016/claude-code-studio
claude-code-chat
# Відкрийте http://localhost:3000
```

**Як оновити:**
```bash
npm install -g github:Lexus2016/claude-code-studio@latest
```

---

### Спосіб 2 — git clone (повний контроль)

**Вимоги:**
- Node.js 18+
- [`claude` CLI](https://docs.anthropic.com/en/claude-code) встановлений та авторизований (для CLI режиму)

```bash
git clone https://github.com/Lexus2016/claude-code-studio.git
cd claude-code-studio
npm install

cp .env.example .env
# Відредагуйте .env за потреби

node server.js
# Відкрийте http://localhost:3000
# Перший запуск: встановіть пароль
```

**Як оновити:**
```bash
git pull
npm install
node server.js
```

---

### Спосіб 3 — Docker

```bash
git clone https://github.com/Lexus2016/claude-code-studio.git
cd claude-code-studio

cp .env.example .env
# Налаштуйте .env за потреби

docker compose up -d --build
docker compose logs -f claude-chat
# Відкрийте http://localhost:3000
```

**Як оновити:**
```bash
git pull
docker compose up -d --build
```

---

## Структура проекту

```
claude-code-studio/
├── server.js           # Express + WebSocket сервер (головна точка входу)
├── auth.js             # bcrypt авторизація, 30-денні токен-сесії
├── claude-cli.js       # Запуск claude CLI підпроцесу, парсинг JSON стріму
├── config.json         # Визначення MCP серверів + каталог skills
├── config.example.json # Шаблон конфігу (закомічений у git)
├── package.json
├── Dockerfile
├── docker-compose.yml
├── .env.example        # Шаблон змінних оточення
├── bin/
│   └── cli.js          # Точка входу для npx / глобального встановлення
├── public/
│   ├── index.html      # Однофайловий SPA (вбудований CSS + JS)
│   └── auth.html       # Сторінка логіна / налаштування
├── skills/             # Skill .md файли (завантажуються в системний промпт)
├── scripts/
│   └── install-hooks.js  # postinstall: мержить Claude Code хуки в .claude/settings.json
├── .claude/
│   ├── settings.json   # Конфіг хуків Claude Code (керується postinstall)
│   ├── scripts/
│   │   ├── file-lock.sh    # PreToolUse хук: чекає лок перед редагуванням
│   │   └── file-unlock.sh  # PostToolUse хук: знімає лок після редагування
│   └── locks/          # Файли блокування рантайму (в .gitignore)
├── data/               # Дані рантайму (в .gitignore)
│   ├── chats.db        # SQLite база даних
│   ├── auth.json       # bcrypt хеш пароля
│   ├── sessions-auth.json
│   ├── projects.json   # Збережений список проектів
│   └── uploads/        # Завантажені файли
└── workspace/          # Стандартна робоча директорія Claude Code (в .gitignore)
```

---

## Конфігурація

### Змінні оточення (`.env`)

```env
PORT=3000
SESSION_SECRET=           # Генерується автоматично якщо порожнє
WORKDIR=./workspace       # Стандартна робоча директорія Claude
TRUST_PROXY=false         # Встановити true за nginx/Caddy
LOG_LEVEL=info            # Рівень логування: error | warn | info | debug
NODE_ENV=production       # Вмикає структурований JSON-лог (для Loki, Datadog тощо)
MAX_TASK_WORKERS=5        # Макс. кількість паралельних процесів Claude Code для Kanban
```

### Додавання MCP серверів
1. Ліва панель → ⚡ MCP → "+ Додати MCP"
2. Або редагувати `config.json` через ⚙️ Config Editor

### Додавання Skills
1. Ліва панель → 🧠 Skills → "+ Upload .md"
2. Або додати `.md` файли в `skills/` та оновити `config.json`
3. Глобальні skills з `~/.claude/skills/` завантажуються автоматично

### Проекти
- Ліва панель → 🗂 Проекти → "+ Новий проект"
- Кожен проект має ім'я та робочу директорію (будь-який шлях на диску)
- Опційна ініціалізація `git init` при створенні
- Сесії прив'язані до проекту — історія фільтрується по проекту

---

## Архітектура

```
Клієнт (браузер) ──WS──► server.js ──► claude-cli.js ──► claude (підпроцес)
                    HTTP ◄───────────────────────────────────────────────────
```

- Єдиний Node.js процес, без інструментів збірки
- WebSocket для двостороннього стрімінгу
- SQLite (WAL режим) для сесій та повідомлень
- Мульти-агент: оркестратор генерує JSON план → паралельне виконання агентів
- Паралелізм вкладок: кожна вкладка браузера має власну незалежну чергу

### SQLite схема

```sql
sessions: id, title, created_at, updated_at, claude_session_id,
          active_mcp, active_skills, mode, agent_mode, model, engine, workdir

messages: id, session_id, role, type, content, tool_name,
          agent_id, reply_to_id, created_at
```

---

## Kanban дошка

Перейдіть на `/kanban` для доступу до дошки завдань.

- **Колонки**: Backlog → До виконання → В процесі → Виконано / Скасовано
- **Задачі**: назва, опис, нотатки, прив'язана сесія, налаштування моделі/режиму
- **Вкладення**: зображення або текстові файли записуються у `.kanban-attachments/{taskId}/` при старті
- **Автозапуск**: переміщення картки у "До виконання" запускає Claude Code автоматично
- **Зупинка**: перетягніть картку з "В процесі" → SIGTERM до процесу Claude
- **Retry**: авто-перезапуск після краша з лічильником без дублювання повідомлень

### Сесії в Kanban: паралельне та послідовне виконання

**Сесія** в Kanban — це те саме, що **чат** у режимі Chat. Вона зберігає весь контекст розмови — все, що Claude вже бачив і обговорював.

При створенні задачі в Kanban ви обираєте: створити **нову сесію** або вибрати **існуючу**. Цей вибір принципово змінює спосіб виконання задачі:

| | Нова сесія | Існуюча сесія |
|---|---|---|
| **Контекст** | Чистий — Claude нічого не знає про попередні задачі | Спільний — Claude пам'ятає всі попередні задачі в цій сесії |
| **Виконання** | **Паралельне** — задача виконується незалежно від інших | **Послідовне** — задача чекає завершення попередньої в тій самій сесії |
| **Підходить для** | Незалежних задач, які не пов'язані між собою | Покрокової роботи, де кожна задача будується на попередній |

**Приклад — Паралельне виконання (нові сесії):**

Потрібно додати сторінку логіна І перероблити дашборд. Ці задачі не пов'язані, тому створюємо дві картки, кожну з **новою сесією**:

```
Картка 1: "Додати сторінку логіна"      → Нова сесія (Сесія #12)
Картка 2: "Переробити дашборд"           → Нова сесія (Сесія #13)
```

Обидві картки виконуються **одночасно** у різних процесах Claude Code. Кожен екземпляр Claude має чистий контекст і не знає про іншу задачу. *(Обмежено параметром `MAX_TASK_WORKERS` — за замовчуванням 5 паралельних процесів.)*

**Приклад — Послідовне виконання (одна сесія):**

Ви хочете, щоб Claude спочатку створив API, а потім написав для нього тести. Друга задача потребує контексту першої. Створюємо дві картки, прив'язані до **однієї сесії**:

```
Картка 1: "Створити REST API для /users"        → Нова сесія (Сесія #14)
Картка 2: "Написати тести для /users API"       → Існуюча Сесія #14
```

Спочатку виконується Картка 1. Коли вона завершується і переходить у "Виконано", стартує Картка 2 — **у тій самій сесії**. Claude вже знає структуру API, розташування файлів та рішення з Картки 1, тому пише тести маючи повний контекст. Це як продовжити розмову в режимі Chat.

> **Підказка:** Подумайте так — якщо б ви задавали ці питання в **одному чаті** (послідовно), прив'яжіть картки до однієї сесії. Якщо б ви відкрили **окремі чати** (паралельно), створіть нову сесію для кожної картки.

---

## Безпека мульти-екземплярів

Коли ви відкриваєте Claude Code у кількох терміналах і одночасно працюєте над одним проектом, два екземпляри можуть випадково редагувати один і той самий файл — останній запис мовчки перезаписує зміни іншого.

Цей проект постачає **Claude Code file-lock хуки**, які автоматично вирішують цю проблему:

- **Перед редагуванням** файлу — хук перевіряє, чи не редагує його інший екземпляр Claude Code. Якщо так, **чекає** (перевірка кожні 3 секунди) поки файл звільниться, потім продовжує.
- **Після редагування** — хук одразу знімає блокування, щоб наступний екземпляр що чекає міг продовжити.
- **Застарілі блокування** (якщо сесія Claude впала) виявляються через перевірку PID і знімаються автоматично.

```
Claude A (термінал 1)        Claude B (термінал 2)
──────────────────────       ──────────────────────────────
Edit server.js               Edit server.js  ← одночасно
→ захоплює лок               → бачить лок, починає чекати...
→ редагує файл ✓               ⏳ перевірка кожні 3с
→ знімає лок                 → лок вільний! захоплює його
                             → редагує файл ✓
                             → знімає лок
```

Хуки встановлюються автоматично при `npm install` через `postinstall` скрипт. Скрипт **мержить** хуки в існуючий `.claude/settings.json` — не перезаписує ваші наявні хуки. Працює на macOS та Linux (включаючи Docker).

---

## Безпека

- Паролі хешуються через bcrypt (12 rounds)
- Auth токени: 32-байтний hex, 30-денний TTL, серверне зберігання
- WebSocket захищений `httpOnly` cookie
- API ключі ніколи не передаються на фронтенд
- Helmet.js заголовки безпеки
- Rate limiting на ендпоінтах авторизації

---

## Розробка

```bash
npm run dev    # node --watch server.js (авто-перезавантаження)
npm start      # node server.js (продакшн)
```

Без лінтера, без тестів, без кроку збірки — vanilla JS фронтенд, plain Node.js бекенд.

---

## Ліцензія

MIT
