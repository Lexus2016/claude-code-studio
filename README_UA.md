# Claude Code Studio

**Веб-інтерфейс для Claude Code** — спілкуйтесь з AI, керуйте задачами на Kanban-дошці, запускайте декілька агентів паралельно та автоматизуйте весь робочий процес розробки. Не потрібно вміти програмувати, щоб почати.

> Мови: [English](README.md) | [Українська](README_UA.md) | [Русский](README_RU.md)

---

## Що це таке?

Claude Code Studio — це браузерний воркспейс для роботи з [Claude Code](https://claude.ai/code) — AI-помічником від Anthropic — без використання терміналу. Ви відкриваєте його в браузері, пишете що потрібно зробити, і Claude це робить.

**Уявіть це як панель керування для вашого AI-помічника:**
- 💬 Спілкуйтесь з Claude і спостерігайте за роботою в реальному часі
- 📋 Створюйте картки задач на Kanban-дошці — Claude підхоплює їх і виконує автоматично
- 👥 Запускайте декілька агентів паралельно для різних задач
- 📁 Переглядайте файли, вставляйте зображення, прикріплюйте документи — прямо з браузера
- 🔌 Підключайте MCP-сервери та файли з навичками для розширення можливостей Claude

---

## Демо відео

Дивіться як працює Kanban-дошка: створення картки задачі для нового релізу → Claude Code підхоплює її → виконує задачу → картка переходить в «Виконано». Наприкінці видно GitHub до та після — агент справді випустив нову версію.

![Demo GIF](public/videos/new_release_video.gif)

<video src="public/videos/new_release_video.mp4" controls width="100%"></video>

> **Відео не відтворюється?** [Завантажте та перегляньте напряму](public/videos/new_release_video.mp4)

---

## Скріншоти

### Екран авторизації
Чиста та безпечна сторінка входу. При першому запуску ви самостійно встановлюєте пароль.

![Екран авторизації](public/screenshots/01-login.png)

### Інтерфейс чату
Розмова з Claude Code у реальному часі. Підтримує markdown, блоки коду з підсвіткою синтаксису, перемикачі MCP-серверів, пресети навичок, файловий браузер і вибір моделі — всі в одній темній сторінці.

![Інтерфейс чату](public/screenshots/02-main-chat.png)

### Kanban-дошка
Перетягніть картку у «До виконання» — Claude сам починає роботу. Спостерігайте за прогресом в реальному часі, перезапускайте після збою, прив'язуйте картки до чат-сесій і відстежуйте все в одному місці.

![Kanban дошка](public/screenshots/03-kanban.png)

---

## Швидкий старт (3 кроки)

**Вимоги:** [Node.js 18+](https://nodejs.org) та [`claude` CLI](https://docs.anthropic.com/en/claude-code) встановлений і авторизований.

```bash
# 1. Запустити одразу (без встановлення)
npx github:Lexus2016/claude-code-studio

# 2. Відкрити браузер
# → http://localhost:3000

# 3. Встановити пароль при першому запуску — і починайте працювати
```

Все. Щоб запустити наступного разу — просто повторіть крок 1.

---

## Способи встановлення

### Варіант А — Запуск через npx (найпростіший, без встановлення)

```bash
npx github:Lexus2016/claude-code-studio
```

**Оновлення до останньої версії:**
```bash
npx github:Lexus2016/claude-code-studio@latest
```

---

### Варіант Б — Глобальне встановлення (запускати будь-коли)

```bash
npm install -g github:Lexus2016/claude-code-studio
claude-code-studio
```

**Оновлення:**
```bash
npm install -g github:Lexus2016/claude-code-studio@latest
```

---

### Варіант В — Клонування репозиторію (для розробників)

```bash
git clone https://github.com/Lexus2016/claude-code-studio.git
cd claude-code-studio
npm install
cp .env.example .env
node server.js
```

Відкрийте `http://localhost:3000` у браузері.

**Оновлення:**
```bash
git pull && npm install && node server.js
```

---

### Варіант Г — Docker

```bash
git clone https://github.com/Lexus2016/claude-code-studio.git
cd claude-code-studio
cp .env.example .env
docker compose up -d --build
```

Відкрийте `http://localhost:3000`. Логи: `docker compose logs -f claude-chat`

**Оновлення:**
```bash
git pull && docker compose up -d --build
```

---

## Що вміє програма?

| Функція | Що це означає для вас |
|---------|-----------------------|
| 💬 **Чат у реальному часі** | Спілкуйтесь з Claude, відповіді з'являються потоково в міру роздумів |
| 📋 **Kanban-дошка** | Створіть картки задач → Claude виконує їх автоматично, по одній або паралельно |
| 👥 **Мульти-агентний режим** | Claude створює команду спеціалізованих агентів і координує їх для великих задач |
| 🚀 **Task Dispatch** | Декомпозиція плану в ланцюжок Kanban-задач з залежностями — авто-повтор при збої |
| ↗️ **Fork Conversation** | Продовжуйте будь-яке повідомлення в новому чаті з повним контекстом |
| ⚡ **MCP-сервери** | Підключайте зовнішні інструменти (бази даних, GitHub, Slack тощо) |
| 🧠 **Skills (навички)** | Завантажуйте `.md` файли, що навчають Claude вашій специфіці |
| 📁 **Файловий браузер** | Переглядайте файли проекту, переглядайте їх, прикріплюйте через `@ім'яфайлу` |
| 🖼 **Vision** | Вставляйте скріншоти з буфера обміну — Claude бачить і аналізує зображення |
| 🗂 **Проекти** | Окремі проекти з власними директоріями та історією чатів |
| 🌐 **Remote SSH Projects** | Підключайтеся до віддалених серверів, створюйте проекти там, запускайте Claude на віддалених машинах |
| 🔄 **Auto-Continue** | Агент досяг ліміту кроків? Він автоматично продовжує роботу (до 3 разів) |
| 💾 **Історія** | Всі сесії збережені в SQLite — продовжуйте будь-яку розмову з того місця де зупинились |
| 📊 **Rate Limit Alerts** | Toast-попередження на 80/90/95%, модальне вікно при вичерпанні, живий таймер до скидання |
| 🔒 **Авторизація** | Логін з паролем + 30-денні токени. Всі дані залишаються на вашій машині |
| 🌍 **i18n** | Інтерфейс українською, англійською або російською (автовизначення) |
| 🐳 **Docker** | Розгортайте будь-де за допомогою Dockerfile та docker-compose |

---

## Чому Claude Code Studio?

Можна використовувати `claude` в терміналі. Навіщо ця програма?

### 🖱️ Візуальне > Термінал

| Термінал | Claude Code Studio |
|----------|-------------------|
| Друкувати довгі команди | Натискати кнопки, перетягувати картки |
| Скролити текст | Бачити прогрес в картках |
| Немає управління задачами | Вбудована Kanban-дошка |
| Одна сесія за раз | Кілька паралельних сесій |
| Копіювати-вставляти скріншоти | Вставляти одразу в чат |
| Пам'ятати контекст самому | Сесії зберігаються автоматично |

### ⚡ Прискорення роботи

- **Kanban-дошка**: Поставте 10 задач у чергу, відійдіть, поверніться — все готово
- **Мульти-агент**: Велика задача? Claude створить команду і координує їх
- **Fork conversations**: Відгалужуйтесь від будь-якої точки без втрати контексту
- **Проекти**: Перемикайтесь між кодовими базами без втрати місця
- **Auto-continue**: Агент досяг ліміту кроків посеред задачі? Він автоматично продовжить — без ручного втручання
- **Збереження сесій**: Поверніться завтра, продовжуйте саме з того місця

### 🎯 Створено для робочих процесів

Термінал чудовий для швидких питань. Але коли є:
- Кілька задач для відстеження
- Тривалі операції
- Скріншоти для аналізу
- Паралельні потоки роботи
- Команда якій потрібна видимість

...потрібен **дашборд**, а не командний рядок.

---

## Kanban-дошка — як це працює

Перейдіть на `/kanban` у браузері (або натисніть іконку дошки на бічній панелі).

### Базовий флоу
1. **Створіть картку** — дайте назву і опишіть що потрібно зробити
2. **Перемістіть у «До виконання»** — Claude підхоплює її автоматично і починає роботу
3. **Спостерігайте за прогресом** — натисніть на картку щоб побачити вивід чату в реальному часі
4. **Картка переходить у «Виконано»** — коли задача виконана, картка рухається сама

### Паралельне vs послідовне виконання

При створенні картки ви обираєте: **нова сесія** або **існуюча**:

| | Нова сесія | Та сама сесія що й інша картка |
|---|---|---|
| **Контекст** | Чистий старт — Claude не знає про інші задачі | Спільний — Claude пам'ятає що робила попередня картка |
| **Виконання** | **Паралельне** з іншими картками нових сесій | **Послідовне** — чекає завершення попередньої картки |
| **Використовуйте коли** | Задачі незалежні одна від одної | Одна задача потребує результату іншої |

**Приклад — паралельне (незалежні задачі):**
```
Картка: "Додати сторінку входу"      → Нова сесія
Картка: "Переробити дашборд"         → Нова сесія
```
Обидві виконуються одночасно в окремих процесах Claude.

**Приклад — послідовне (залежні задачі):**
```
Картка: "Створити API /users"        → Нова сесія #14
Картка: "Написати тести для API"     → Існуюча сесія #14
```
Картка 2 чекає поки Картка 1 завершиться, потім виконується з повним контекстом того що було побудовано.

> **Правило:** якщо ви б задали ці питання в одному чаті — прив'яжіть до однієї сесії. Якщо б відкрили два окремих чати — використовуйте нові сесії.

---

## Віддалений доступ: для розробників і системних адміністраторів

Claude Code Studio підключається до віддалених серверів через SSH і запускає там Claude Code — ніби це локальна машина. **Всі команди виконуються на віддаленій машині.** Це відкриває два принципово різних сценарії використання.

### 👨‍💻 Для розробників — Віддалені проекти

Працюйте над кодом що знаходиться на потужному сервері (staging, GPU, cloud VPS) не виходячи з браузера:

| Сценарій | Перевага |
|----------|----------|
| **Потужні машини для збірки** | Компіляція, тренування, тести на GPU/high-RAM — ноутбук не сповільнюється |
| **Production-паритет** | Claude Code запускається в тому ж середовищі де код реально працює |
| **Агент 24/7** | Залиште задачу на ніч — вранці перевірте результат |
| **Спільна видимість команди** | Один екземпляр Claude Code Studio — вся команда бачить ті самі проекти |

### 🖥️ Для системних адміністраторів — Керування серверами

Тут Claude Code Studio стає чимось, чого ще не пропонував жоден інструмент системного адміністрування: **розмовним інтерфейсом до вашого серверного парку.**

Встановіть Claude Code на кожен сервер який адмініструєте. Додайте їх як SSH-хости. Тепер замість того щоб заходити на сервер через SSH і вводити команди вручну — відкриваєте браузер і **спілкуєтесь з Claude Code що запущений безпосередньо на сервері**.

| Що ви делегуєте Claude | Що Claude робить на сервері |
|------------------------|----------------------------|
| «Онови nginx до 1.27 і перезавантаж» | Перевіряє версію, оновлює пакет, тестує конфіг, перезавантажує сервіс |
| «Перевір диск і очисти старі логи» | Запускає `df`, знаходить великі файли, безпечно видаляє, звітує про звільнений простір |
| «Задеплой новий реліз, при провалі health-check — відкати» | Пулить, білдить, рестартує, перевіряє, авто-відкат при помилці |
| «Що зараз їсть CPU?» | Запускає `top`, аналізує, пояснює, пропонує рішення |
| «Проротуй API-ключі в усіх конфігах» | Знаходить всі входження, атомарно оновлює, рестартує залежні сервіси |
| «Налаштуй cron для нічного бекапу БД» | Пише скрипт, додає cron, перевіряє реєстрацію |

**Результат:** Ви керуєте всім серверним парком з однієї вкладки браузера. Без термінального жонглювання. Без SSH-сесій. Лише розмова.

```
Звичайна сесія адміна:                Claude Code Studio:
──────────────────────                ─────────────────────────────────────
ssh user@prod-eu-01                   Відкрив браузер
sudo apt update && apt upgrade -y     Написав: «Онови всі пакети на prod-eu-01,
sudo systemctl restart nginx                   перевір помилки, дай підсумок»
sudo journalctl -u nginx -n 50        Claude: виконує, показує вивід, відмічає проблеми
exit
ssh user@prod-eu-02                   Написав: «Те саме на prod-eu-02 і prod-us-01»
... і так для кожного сервера ...     Claude: запускає паралельно на обох серверах
```

### Швидкий старт

**Крок 0 — Підготовка сервера (один раз):**

Використовуйте вбудовані скрипти встановлення для інсталяції Claude Code на віддаленій машині:

```bash
# Ubuntu / Debian / RHEL / CentOS (від root):
curl -fsSL https://raw.githubusercontent.com/Lexus2016/claude-code-studio/main/install/setup-linux.sh | bash

# macOS:
curl -fsSL https://raw.githubusercontent.com/Lexus2016/claude-code-studio/main/install/setup-macos.sh | bash

# Windows (PowerShell від адміністратора):
irm https://raw.githubusercontent.com/Lexus2016/claude-code-studio/main/install/setup-windows.ps1 | iex
```

Кожен скрипт встановлює Node.js, Claude Code CLI, налаштовує авторизацію та створює workspace. Деталі в [`install/README.md`](install/README.md).

**Кроки:**

1. **Додайте SSH-хост** (один раз на сервер)
   - Ліва панель → ⚙️ Налаштування → **Віддалені хости**
   - Натисніть **+ Додати хост** → введіть хостнейм, порт, SSH-ключ
   - Натисніть **Test Connection** для перевірки

2. **Створіть Віддалений проект**
   - Ліва панель → 🗂 Проекти → **+ Новий проект**
   - Тип **Remote** → оберіть SSH-хост
   - Введіть шлях до директорії на сервері
   - Натисніть **Створити**

3. **Працюйте звичайно**
   - Чат, Kanban, файловий браузер — все виконується на сервері
   - Сесії та історія зберігаються локально в Claude Code Studio

### Приклад: 5 production-серверів

```
Мета: оновити nginx на 5 серверах і перевірити.

1. Додайте 5 хостів: prod-eu-01 ... prod-eu-05
2. Створіть Віддалений проект для кожного → /etc/nginx
3. Відкрийте 5 Kanban-карток паралельно:
   → «Онови nginx до 1.27, протестуй конфіг, перезавантаж, health-check»
4. Всі 5 виконуються одночасно. Live-вивід по кожному серверу.
5. Збій → картка залишається в «В процесі» з деталями помилки

Результат: 5 серверів оновлено і перевірено за ~3 хвилини, без жодного SSH вручну.
```

### Приклад: GPU-сервер для ML-задач

```
У вас є потужний ML-сервер (GPU, 256GB RAM).
Claude Code Studio на вашому ноутбуці не може впоратися з навчанням локально.

→ Додайте віддалений хост: ml-server.internal
→ Створіть Віддалений проект → /home/ml/projects
→ Чат: «Завантаж датасет, препроцесуй, тренуй модель»
→ Claude запускається на ml-server: використовує GPU, доступ до даних локально, завершує швидко
→ Ви бачите live-вивід у браузері — ваш ноутбук працює нормально
```

### SSH-безпека

- **Паролі зашифровані:** SSH-паролі зберігаються за допомогою AES-256-GCM шифрування — ніколи у відкритому вигляді. Ключ шифрування знаходиться в `data/hosts.key` (не потрапляє в git)
- **SSH-ключі зберігаються локально:** Файли ключів залишаються на диску; використовуються лише під час підключення, не копіюються і не зберігаються деінде
- **Без перенаправлення ключів:** Ключі використовуються лише для встановлення з'єднання
- **Тестування з'єднання:** Завжди перевіряйте перед створенням проектів

---

## Захист від конфліктів (файлові блокування)

Якщо запустити кілька процесів Claude Code на одному проекті одночасно, вони можуть спробувати редагувати один файл паралельно — і останній запис мовчки перезапише зміни іншого.

Claude Code Studio вирішує це автоматично через **file-lock хуки**:

- Перед редагуванням файлу → перевіряється чи не редагує його вже інший процес Claude
- Якщо так → чекає поки файл звільниться (перевірка кожні 3 секунди)
- Після редагування → одразу знімає блокування
- Збої процесів → застарілі блокування виявляються через PID і знімаються автоматично

```
Claude A (задача 1)         Claude B (задача 2)
──────────────────          ────────────────────────────
Edit server.js              Edit server.js  ← одночасно
→ захоплює блокування       → чекає (зайнято)
→ редагує файл ✓             ⏳ перевірка кожні 3с...
→ знімає блокування         → вільно! захоплює блокування
                            → редагує файл ✓
```

Хуки встановлюються автоматично при `npm install`. Працює на macOS, Linux та Docker.

---

## Конфігурація

### Змінні оточення (`.env`)

Скопіюйте `.env.example` у `.env` та змініть за потребою:

```env
PORT=3000                    # Порт для запуску
WORKDIR=./workspace          # Де Claude працює (ваші файли проекту)
MAX_TASK_WORKERS=5           # Макс. паралельних процесів Claude для Kanban
CLAUDE_TIMEOUT_MS=1800000    # Максимальний час виконання задачі в мс (за замовч.: 30 хвилин)
TRUST_PROXY=false            # Встановіть true якщо за nginx/Caddy
LOG_LEVEL=info               # Логування: error | warn | info | debug
```

Для SDK-движка (необов'язково, якщо хочете використовувати API-ключ замість `claude` CLI):
```env
ANTHROPIC_API_KEY=sk-ant-...
```

### Додавання MCP-серверів
Ліва панель → ⚡ MCP → **+ Додати MCP**

### Додавання Skills
Ліва панель → 🧠 Skills → **+ Upload .md**

Або додайте `.md` файли напряму в папку `skills/` і перезапустіть.

### Проекти
Ліва панель → 🗂 Проекти → **+ Новий проект**

Кожен проект має власну робочу директорію. Сесії прив'язані до проектів.

---

## Безпека

- Паролі хешуються через bcrypt (12 rounds) — ніколи не зберігаються у відкритому вигляді
- Auth-токени: 32-байтний випадковий hex, термін дії 30 днів, серверне зберігання
- WebSocket захищений `httpOnly` cookie
- API-ключі ніколи не передаються в браузер
- **SSH-паролі зашифровані AES-256-GCM** — ключ зберігається в `data/hosts.key`, ніколи не в базі даних
- Заголовки безпеки Helmet.js на всіх відповідях
- Rate limiting на ендпоінтах входу

---

## Розробка

```bash
npm run dev    # авторестарт при зміні файлів (node --watch)
npm start      # запуск для продакшну
```

Без інструментів збірки, без TypeScript, без конфігурацій лінтингу — vanilla JS фронтенд, plain Node.js бекенд. Відкрийте `public/index.html` щоб побачити весь фронтенд.

---

## Структура проекту

```
claude-code-studio/
├── server.js           ← головний сервер (Express + WebSocket)
├── auth.js             ← вхід, хешування паролю, сесійні токени
├── claude-cli.js       ← запуск підпроцесу claude, парсинг вихідного потоку
├── claude-ssh.js       ← SSH віддалене виконання (ssh2, паролі зашифровані)
├── config.json         ← MCP-сервери + каталог навичок
├── public/
│   ├── index.html      ← весь фронтенд (один файл: HTML + CSS + JS)
│   └── auth.html       ← сторінка входу/налаштування
├── skills/             ← .md файли навичок для системного промпту Claude
├── data/               ← дані рантайму (в .gitignore)
│   ├── chats.db        ← SQLite база даних (сесії + повідомлення)
│   └── auth.json       ← хеш пароля
└── workspace/          ← стандартна робоча директорія Claude (в .gitignore)
```

---

## Ліцензія

MIT
