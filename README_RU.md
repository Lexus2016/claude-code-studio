# Claude Code Studio

**Полнофункциональный веб-воркспейс для [Claude Code](https://claude.ai/code)** — чат, Kanban доска задач, мульти-агентная оркестрация, MCP серверы, Skills, проекты — всё в одном месте. Без шага сборки.

> Языки: [English](README.md) | [Українська](README_UA.md) | [Русский](README_RU.md)

---

## Возможности

| Функция | Описание |
|---------|----------|
| 🖥 CLI Mode | Через `claude` CLI с Max подпиской (без затрат на API) |
| 💬 Чат в реальном времени | WebSocket стриминг с рендерингом markdown |
| 👥 Мульти-Агент | Оркестрация команды агентов с графом зависимостей |
| ⚡ MCP Серверы | Подключение любых MCP серверов — пресеты + кастомные |
| 🧠 Skills | Загрузка `.md` skill файлов в системный промпт Claude |
| 🔄 Режимы | Auto / Planning / Task режимы выполнения |
| 💎 Модели | Opus 4.6 / Sonnet 4.6 / Haiku 4.5 |
| 📁 Файловый браузер | Просмотр workspace, превью файлов, прикрепление через `@mention` |
| 🖼 Vision | Вставка изображений из буфера обмена как vision блоков |
| 📋 История | Постоянные сессии в SQLite с возможностью восстановления |
| ⚙️ Config Editor | Редактирование `config.json`, `CLAUDE.md`, `.env` прямо в UI |
| 🔒 Auth | bcrypt пароль + 30-дневные сессионные токены |
| 🐳 Docker | Dockerfile + docker-compose в комплекте |
| 🗂 Проекты | Именованные проекты с кастомным workdir, git init, браузер директорий |
| 🪟 Параллельные вкладки | Несколько вкладок независимо; стриминг сохраняется при переключении |
| 📊 Статистика | Дневное/недельное использование Claude Max, оценка контекстных токенов |
| 🔐 Безопасность мульти-экземпляров | File-lock хуки; workdir-очередь в Kanban |
| 📋 Kanban доска | Drag-and-drop доска на `/kanban` — задачи запускают Claude Code автоматически |
| 📎 Вложения файлов | Прикрепление изображений и текстовых файлов к карточкам Kanban |
| 🔁 Retry | Авто-перезапуск задачи после краша со счётчиком; без дублирования сообщений |
| 🌍 i18n | Интерфейс на Русском / Английском / Украинском (автоопределение) |

---

## Скриншоты

### Экран авторизации
Безопасная аутентификация с bcrypt-хешированием паролей и 30-дневными сессионными токенами.

![Экран авторизации](public/screenshots/01-login.png)

### Интерфейс чата
Полнофункциональный воркспейс: чат в реальном времени через WebSocket с рендерингом markdown, переключатели MCP серверов, 20+ пресетов навыков (автоподбор по задаче), файловый браузер с превью, выбор модели и режима — всё в одном dark-themed SPA.

![Интерфейс чата](public/screenshots/02-main-chat.png)

### Kanban доска
Drag-and-drop доска задач с автоматическим запуском Claude Code. Переместите карточку в «К выполнению» — Claude берёт задачу, выполняет её и отчитывается. Авто-retry при краше, фильтрация по проектам, выбор модели на каждую карточку.

![Kanban доска](public/screenshots/03-kanban.png)

---

## Установка и запуск

### Способ 1 — Запуск через npx (без установки)

Самый простой способ. Скачивает и запускает последний релиз сразу:

```bash
npx github:Lexus2016/claude-code-studio
# Откройте http://localhost:3000
```

Или установите глобально и запускайте в любое время:

```bash
npm install -g github:Lexus2016/claude-code-studio
claude-code-chat
# Откройте http://localhost:3000
```

**Как обновить:**
```bash
npm install -g github:Lexus2016/claude-code-studio@latest
```

---

### Способ 2 — git clone (полный контроль)

**Требования:**
- Node.js 18+
- [`claude` CLI](https://docs.anthropic.com/en/claude-code) установлен и авторизован (для CLI режима)

```bash
git clone https://github.com/Lexus2016/claude-code-studio.git
cd claude-code-studio
npm install

cp .env.example .env
# Отредактируйте .env по необходимости

node server.js
# Откройте http://localhost:3000
# Первый запуск: установите пароль
```

**Как обновить:**
```bash
git pull
npm install
node server.js
```

---

### Способ 3 — Docker

```bash
git clone https://github.com/Lexus2016/claude-code-studio.git
cd claude-code-studio

cp .env.example .env
# Настройте .env по необходимости

docker compose up -d --build
docker compose logs -f claude-chat
# Откройте http://localhost:3000
```

**Как обновить:**
```bash
git pull
docker compose up -d --build
```

---

## Структура проекта

```
claude-code-studio/
├── server.js           # Express + WebSocket сервер (главная точка входа)
├── auth.js             # bcrypt авторизация, 30-дневные токен-сессии
├── claude-cli.js       # Запуск claude CLI подпроцесса, парсинг JSON стрима
├── config.json         # Определения MCP серверов + каталог skills
├── config.example.json # Шаблон конфига (закоммичен в git)
├── package.json
├── Dockerfile
├── docker-compose.yml
├── .env.example        # Шаблон переменных окружения
├── bin/
│   └── cli.js          # Точка входа для npx / глобальной установки
├── public/
│   ├── index.html      # Однофайловый SPA (встроенный CSS + JS)
│   └── auth.html       # Страница логина / настройки
├── skills/             # Skill .md файлы (загружаются в системный промпт)
├── scripts/
│   └── install-hooks.js  # postinstall: мержит Claude Code хуки в .claude/settings.json
├── .claude/
│   ├── settings.json   # Конфиг хуков Claude Code (управляется postinstall)
│   ├── scripts/
│   │   ├── file-lock.sh    # PreToolUse хук: ждёт лок перед редактированием
│   │   └── file-unlock.sh  # PostToolUse хук: снимает лок после редактирования
│   └── locks/          # Файлы блокировки рантайма (в .gitignore)
├── data/               # Данные рантайма (в .gitignore)
│   ├── chats.db        # SQLite база данных
│   ├── auth.json       # bcrypt хеш пароля
│   ├── sessions-auth.json
│   ├── projects.json   # Сохранённый список проектов
│   └── uploads/        # Загруженные файлы
└── workspace/          # Рабочая директория Claude Code по умолчанию (в .gitignore)
```

---

## Конфигурация

### Переменные окружения (`.env`)

```env
PORT=3000
SESSION_SECRET=           # Генерируется автоматически если пусто
WORKDIR=./workspace       # Рабочая директория Claude по умолчанию
TRUST_PROXY=false         # Установить true за nginx/Caddy
LOG_LEVEL=info            # Уровень логирования: error | warn | info | debug
NODE_ENV=production       # Включает структурированный JSON-лог (для Loki, Datadog и др.)
MAX_TASK_WORKERS=5        # Макс. количество параллельных процессов Claude Code для Kanban
CLAUDE_TIMEOUT_MS=1800000 # Таймаут подпроцесса Claude в мс (по умолч. 30 мин; увелич. для долгих агентских задач)
```

### Добавление MCP серверов
1. Левая панель → ⚡ MCP → "+ Добавить MCP"
2. Или редактировать `config.json` через ⚙️ Config Editor

### Добавление Skills
1. Левая панель → 🧠 Skills → "+ Upload .md"
2. Или добавить `.md` файлы в `skills/` и обновить `config.json`
3. Глобальные skills из `~/.claude/skills/` загружаются автоматически

### Проекты
- Левая панель → 🗂 Проекты → "+ Новый проект"
- Каждый проект имеет имя и рабочую директорию (любой путь на диске)
- Опциональная инициализация `git init` при создании
- Сессии привязаны к проекту — история фильтруется по проекту

---

## Архитектура

```
Клиент (браузер) ──WS──► server.js ──► claude-cli.js ──► claude (подпроцесс)
                    HTTP ◄───────────────────────────────────────────────────
```

- Единственный Node.js процесс, без инструментов сборки
- WebSocket для двустороннего стриминга
- SQLite (WAL режим) для сессий и сообщений
- Мульти-агент: оркестратор генерирует JSON план → параллельное выполнение агентов
- Параллелизм вкладок: каждая вкладка браузера имеет собственную независимую очередь

### SQLite схема

```sql
sessions: id, title, created_at, updated_at, claude_session_id,
          active_mcp, active_skills, mode, agent_mode, model, engine, workdir

messages: id, session_id, role, type, content, tool_name,
          agent_id, reply_to_id, created_at
```

---

## Kanban доска

Перейдите на `/kanban` для доступа к доске задач.

- **Колонки**: Backlog → К выполнению → В процессе → Выполнено / Отменено
- **Задачи**: название, описание, заметки, связанная сессия, настройки модели/режима
- **Вложения**: изображения и текстовые файлы записываются в `.kanban-attachments/{taskId}/` при старте
- **Автозапуск**: перемещение карточки в «К выполнению» запускает Claude Code автоматически
- **Остановка**: перетащите карточку из «В процессе» → SIGTERM к процессу Claude
- **Retry**: авто-перезапуск после краша со счётчиком без дублирования сообщений

### Сессии в Kanban: параллельное и последовательное выполнение

**Сессия** в Kanban — это то же самое, что **чат** в режиме Chat. Она хранит весь контекст разговора — всё, что Claude уже видел и обсуждал.

При создании задачи в Kanban вы выбираете: создать **новую сессию** или выбрать **существующую**. Этот выбор принципиально меняет способ выполнения задачи:

| | Новая сессия | Существующая сессия |
|---|---|---|
| **Контекст** | Чистый — Claude ничего не знает о предыдущих задачах | Общий — Claude помнит все предыдущие задачи в этой сессии |
| **Выполнение** | **Параллельное** — задача выполняется независимо от других | **Последовательное** — задача ждёт завершения предыдущей в той же сессии |
| **Подходит для** | Независимых задач, не связанных друг с другом | Пошаговой работы, где каждая задача строится на предыдущей |

**Пример — Параллельное выполнение (новые сессии):**

Нужно добавить страницу логина И переделать дашборд. Задачи не связаны, поэтому создаём две карточки, каждую с **новой сессией**:

```
Карточка 1: "Добавить страницу логина"      → Новая сессия (Сессия #12)
Карточка 2: "Переделать дашборд"            → Новая сессия (Сессия #13)
```

Обе карточки выполняются **одновременно** в разных процессах Claude Code. Каждый экземпляр Claude имеет чистый контекст и не знает о другой задаче. *(Ограничено параметром `MAX_TASK_WORKERS` — по умолчанию 5 параллельных процессов.)*

**Пример — Последовательное выполнение (одна сессия):**

Вы хотите, чтобы Claude сначала создал API, а затем написал для него тесты. Вторая задача требует контекста первой. Создаём две карточки, привязанные к **одной сессии**:

```
Карточка 1: "Создать REST API для /users"        → Новая сессия (Сессия #14)
Карточка 2: "Написать тесты для /users API"      → Существующая Сессия #14
```

Сначала выполняется Карточка 1. Когда она завершается и переходит в «Выполнено», стартует Карточка 2 — **в той же сессии**. Claude уже знает структуру API, расположение файлов и решения из Карточки 1, поэтому пишет тесты имея полный контекст. Это как продолжить разговор в режиме Chat.

> **Подсказка:** Подумайте так — если бы вы задавали эти вопросы в **одном чате** (последовательно), привяжите карточки к одной сессии. Если бы вы открыли **отдельные чаты** (параллельно), создайте новую сессию для каждой карточки.

---

## Безопасность мульти-экземпляров

Когда вы открываете Claude Code в нескольких терминалах и одновременно работаете над одним проектом, два экземпляра могут случайно редактировать один и тот же файл — последняя запись молча перезаписывает изменения другого.

Этот проект поставляется с **Claude Code file-lock хуками**, которые автоматически решают эту проблему:

- **Перед редактированием** файла — хук проверяет, не редактирует ли его другой экземпляр Claude Code. Если да, **ждёт** (проверка каждые 3 секунды) пока файл освободится, затем продолжает.
- **После редактирования** — хук сразу снимает блокировку, чтобы следующий ожидающий экземпляр мог продолжить.
- **Устаревшие блокировки** (если сессия Claude упала) обнаруживаются через проверку PID и снимаются автоматически.

```
Claude A (терминал 1)        Claude B (терминал 2)
──────────────────────       ──────────────────────────────
Edit server.js               Edit server.js  ← одновременно
→ захватывает лок            → видит лок, начинает ждать...
→ редактирует файл ✓           ⏳ проверка каждые 3с
→ снимает лок                → лок свободен! захватывает его
                             → редактирует файл ✓
                             → снимает лок
```

Хуки устанавливаются автоматически при `npm install` через `postinstall` скрипт. Скрипт **мержит** хуки в существующий `.claude/settings.json` — не перезаписывает ваши настроенные хуки. Работает на macOS и Linux (включая Docker).

---

## Безопасность

- Пароли хешируются через bcrypt (12 rounds)
- Auth токены: 32-байтный hex, 30-дневный TTL, серверное хранение
- WebSocket защищён `httpOnly` cookie
- API ключи никогда не передаются на фронтенд
- Helmet.js заголовки безопасности
- Rate limiting на эндпоинтах авторизации

---

## Разработка

```bash
npm run dev    # node --watch server.js (авто-перезагрузка)
npm start      # node server.js (продакшн)
```

Без линтера, без тестов, без шага сборки — vanilla JS фронтенд, plain Node.js бэкенд.

---

## Лицензия

MIT
