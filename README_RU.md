# Claude Code Studio

**Веб-интерфейс для Claude Code** — общайтесь с AI, управляйте задачами на Kanban-доске, запускайте несколько агентов параллельно и автоматизируйте весь рабочий процесс разработки. Не нужно уметь программировать, чтобы начать.

> Языки: [English](README.md) | [Українська](README_UA.md) | [Русский](README_RU.md)

---

## Что это такое?

Claude Code Studio — браузерный воркспейс для работы с [Claude Code](https://claude.ai/code) — AI-помощником от Anthropic — без использования терминала. Вы открываете его в браузере, пишете что нужно сделать, и Claude это делает.

**Представьте это как панель управления для вашего AI-помощника:**
- 💬 Общайтесь с Claude и наблюдайте за работой в реальном времени
- 📋 Создавайте карточки задач на Kanban-доске — Claude подхватывает их и выполняет автоматически
- 👥 Запускайте несколько агентов параллельно для разных задач
- 📁 Просматривайте файлы, вставляйте изображения, прикрепляйте документы — прямо из браузера
- 🔌 Подключайте MCP-серверы и файлы с навыками для расширения возможностей Claude

---

## Демо видео

Смотрите как работает Kanban-доска: создание карточки задачи для нового релиза → Claude Code подхватывает её → выполняет задачу → карточка переходит в «Выполнено». В конце видно GitHub до и после — агент действительно выпустил новую версию.

![Demo GIF](public/videos/new_release_video.gif)

<video src="public/videos/new_release_video.mp4" controls width="100%"></video>

> **Видео не воспроизводится?** [Скачайте и посмотрите напрямую](public/videos/new_release_video.mp4)

---

## Скриншоты

### Экран авторизации
Чистая и безопасная страница входа. При первом запуске вы сами устанавливаете пароль.

![Экран авторизации](public/screenshots/01-login.png)

### Интерфейс чата
Разговор с Claude Code в реальном времени. Поддерживает markdown, блоки кода с подсветкой синтаксиса, переключатели MCP-серверов, пресеты навыков, файловый браузер и выбор модели — всё в одной тёмной странице.

![Интерфейс чата](public/screenshots/02-main-chat.png)

### Kanban-доска
Перетащите карточку в «К выполнению» — Claude сам начинает работу. Наблюдайте за прогрессом в реальном времени, перезапускайте после сбоя, привязывайте карточки к чат-сессиям и отслеживайте всё в одном месте.

![Kanban доска](public/screenshots/03-kanban.png)

---

## Быстрый старт (3 шага)

**Требования:** [Node.js 18+](https://nodejs.org) и [`claude` CLI](https://docs.anthropic.com/en/claude-code) установлен и авторизован.

```bash
# 1. Запустить сразу (без установки)
npx github:Lexus2016/claude-code-studio

# 2. Открыть браузер
# → http://localhost:3000

# 3. Установить пароль при первом запуске — и начинайте работать
```

Всё. Чтобы запустить в следующий раз — просто повторите шаг 1.

---

## Способы установки

### Вариант А — Запуск через npx (самый простой, без установки)

```bash
npx github:Lexus2016/claude-code-studio
```

**Обновление до последней версии:**
```bash
npx github:Lexus2016/claude-code-studio@latest
```

---

### Вариант Б — Глобальная установка (запускать в любое время)

```bash
npm install -g github:Lexus2016/claude-code-studio
claude-code-studio
```

**Обновление:**
```bash
npm install -g github:Lexus2016/claude-code-studio@latest
```

---

### Вариант В — Клонирование репозитория (для разработчиков)

```bash
git clone https://github.com/Lexus2016/claude-code-studio.git
cd claude-code-studio
npm install
cp .env.example .env
node server.js
```

Откройте `http://localhost:3000` в браузере.

**Обновление:**
```bash
git pull && npm install && node server.js
```

---

### Вариант Г — Docker

```bash
git clone https://github.com/Lexus2016/claude-code-studio.git
cd claude-code-studio
cp .env.example .env
docker compose up -d --build
```

Откройте `http://localhost:3000`. Логи: `docker compose logs -f claude-chat`

**Обновление:**
```bash
git pull && docker compose up -d --build
```

---

## Что умеет программа?

| Функция | Что это значит для вас |
|---------|------------------------|
| 💬 **Чат в реальном времени** | Общайтесь с Claude, ответы появляются потоково по мере размышлений |
| 📋 **Kanban-доска** | Создавайте карточки задач → Claude выполняет их автоматически, по одной или параллельно |
| 👥 **Мульти-агентный режим** | Claude создаёт команду специализированных агентов и координирует их для больших задач |
| 🚀 **Task Dispatch** | Декомпозиция плана в цепочку Kanban-задач с зависимостями — авто-повтор при сбое |
| ↗️ **Fork Conversation** | Продолжайте любое сообщение в новом чате с полным контекстом |
| ⚡ **MCP-серверы** | Подключайте внешние инструменты (базы данных, GitHub, Slack и др.) |
| 🧠 **Skills (навыки)** | Загружайте `.md` файлы, обучающие Claude вашей специфике |
| 📁 **Файловый браузер** | Просматривайте файлы проекта, открывайте превью, прикрепляйте через `@имяфайла` |
| 🖼 **Vision** | Вставляйте скриншоты из буфера обмена — Claude видит и анализирует изображения |
| 🗂 **Проекты** | Отдельные проекты со своими директориями и историей чатов |
| 🌐 **Remote SSH Projects** | Подключайтесь к удаленным серверам, создавайте проекты там, запускайте Claude на удаленных машинах |
| 🔄 **Auto-Continue** | Агент достиг лимита шагов? Он автоматически продолжает работу (до 3 раз) |
| 💾 **История** | Все сессии сохранены в SQLite — продолжайте любой разговор с того места, где остановились |
| 📊 **Rate Limit Alerts** | Toast-предупреждения на 80/90/95%, модальное окно при исчерпании, живой таймер до сброса |
| 🔒 **Авторизация** | Вход с паролем + 30-дневные токены. Все данные остаются на вашей машине |
| 🌍 **i18n** | Интерфейс на русском, английском или украинском (автоопределение) |
| 🐳 **Docker** | Разворачивайте где угодно с помощью Dockerfile и docker-compose |

---

## Почему Claude Code Studio?

Можно использовать `claude` в терминале. Зачем эта программа?

### 🖱️ Визуальное > Терминал

| Терминал | Claude Code Studio |
|----------|-------------------|
| Печатать длинные команды | Нажимать кнопки, перетаскивать карточки |
| Скроллить текст | Видеть прогресс в карточках |
| Нет управления задачами | Встроенная Kanban-доска |
| Одна сессия за раз | Несколько параллельных сессий |
| Копировать-вставлять скриншоты | Вставлять сразу в чат |
| Помнить контекст самому | Сессии сохраняются автоматически |

### ⚡ Ускорение работы

- **Kanban-доска**: Поставьте 10 задач в очередь, отойдите, вернитесь — всё готово
- **Мульти-агент**: Большая задача? Claude создаст команду и скоординирует их
- **Fork conversations**: Ответвляйтесь от любой точки без потери контекста
- **Проекты**: Переключайтесь между кодовыми базами без потери места
- **Auto-continue**: Агент достиг лимита шагов посреди задачи? Он автоматически продолжит — без ручного вмешательства
- **Сохранение сессий**: Вернитесь завтра, продолжите именно с того места

### 🎯 Создано для рабочих процессов

Терминал хорош для быстрых вопросов. Но когда есть:
- Несколько задач для отслеживания
- Длительные операции
- Скриншоты для анализа
- Параллельные потоки работы
- Команда которой нужна видимость

...нужен **дашборд**, а не командная строка.

---

## Kanban-доска — как это работает

Перейдите на `/kanban` в браузере (или нажмите иконку доски на боковой панели).

### Базовый флоу
1. **Создайте карточку** — дайте название и опишите что нужно сделать
2. **Переместите в «К выполнению»** — Claude подхватывает её автоматически и начинает работу
3. **Наблюдайте за прогрессом** — нажмите на карточку чтобы увидеть вывод чата в реальном времени
4. **Карточка переходит в «Выполнено»** — когда задача завершена, карточка двигается сама

### Параллельное vs последовательное выполнение

При создании карточки вы выбираете: **новая сессия** или **существующая**:

| | Новая сессия | Та же сессия что и другая карточка |
|---|---|---|
| **Контекст** | Чистый старт — Claude не знает о других задачах | Общий — Claude помнит что делала предыдущая карточка |
| **Выполнение** | **Параллельное** с другими карточками новых сессий | **Последовательное** — ждёт завершения предыдущей карточки |
| **Используйте когда** | Задачи независимы друг от друга | Одна задача требует результата другой |

**Пример — параллельное (независимые задачи):**
```
Карточка: "Добавить страницу входа"  → Новая сессия
Карточка: "Переделать дашборд"       → Новая сессия
```
Обе выполняются одновременно в отдельных процессах Claude.

**Пример — последовательное (зависимые задачи):**
```
Карточка: "Создать API /users"       → Новая сессия #14
Карточка: "Написать тесты для API"   → Существующая сессия #14
```
Карточка 2 ждёт пока Карточка 1 завершится, затем выполняется с полным контекстом того что было построено.

> **Правило:** если вы бы задали эти вопросы в одном чате — привяжите к одной сессии. Если бы открыли два отдельных чата — используйте новые сессии.

---

## Удаленный доступ: для разработчиков и системных администраторов

Claude Code Studio подключается к удаленным серверам через SSH и запускает там Claude Code — как будто это локальная машина. **Все команды выполняются на удаленной машине.** Это открывает два принципиально разных сценария использования.

### 👨‍💻 Для разработчиков — Удаленные проекты

Работайте над кодом что находится на мощном сервере (staging, GPU, облачная VPS) не выходя из браузера:

| Сценарий | Преимущество |
|----------|-------------|
| **Мощные машины для сборки** | Компиляция, обучение, тесты на GPU/high-RAM — ваш ноутбук остается быстрым |
| **Production-паритет** | Claude Code запускается в той же среде где код фактически работает |
| **Агент 24/7** | Оставьте задачу на ночь — утром проверьте результат |
| **Общее видимость команды** | Один экземпляр Claude Code Studio — вся команда видит те же проекты |

### 🖥️ Для системных администраторов — Управление серверами

Здесь Claude Code Studio становится тем, что никогда не предлагал ни один инструмент системного администрирования: **разговорным интерфейсом ко всему вашему серверному парку.**

Установите Claude Code на каждый сервер который администрируете. Добавьте их как SSH-хосты. Теперь вместо того чтобы заходить на сервер через SSH и вводить команды вручную — откройте браузер и **общайтесь с Claude Code запущенным прямо на сервере**.

| Что вы делегируете Claude | Что Claude делает на сервере |
|---------------------------|-------------------------------|
| «Обновите nginx до 1.27 и перезагрузите» | Проверяет версию, обновляет пакет, тестирует конфиг, перезагружает сервис |
| «Проверьте диск и очистите старые логи» | Запускает `df`, находит большие файлы, безопасно удаляет, отчитывается о освобожденном месте |
| «Разверните новый релиз, откатитесь если health-check провалится» | Пулит, собирает, перезагружает, проверяет, автоматический откат при ошибке |
| «Что сейчас потребляет CPU?» | Запускает `top`, анализирует, объясняет, предлагает решение |
| «Поротируйте API-ключи во всех конфигах» | Находит все вхождения, атомарно обновляет, перезагружает зависимые сервисы |
| «Настройте cron для ночной резервной копии БД» | Пишет скрипт, добавляет cron, проверяет регистрацию |

**Результат:** Вы управляете всем серверным парком из одной вкладки браузера. Без жонглирования терминалом. Без SSH-сессий. Только разговор.

```
Обычная сессия администратора:       Claude Code Studio:
──────────────────────────────       ──────────────────────────────────────
ssh user@prod-eu-01                  Открыл браузер
sudo apt update && apt upgrade -y    Написал: «Обновите все пакеты на prod-eu-01,
sudo systemctl restart nginx              проверьте ошибки, дайте мне отчет»
sudo journalctl -u nginx -n 50       Claude: выполняет, показывает вывод, отмечает проблемы
exit
ssh user@prod-eu-02                  Написал: «То же самое на prod-eu-02 и prod-us-01»
... и так для каждого сервера ...   Claude: запускает параллельно на обоих серверах
```

### Быстрый старт

**Шаг 0 — Подготовка сервера (один раз):**

Используйте встроенные скрипты установки для инсталляции Claude Code на удаленную машину:

```bash
# Ubuntu / Debian / RHEL / CentOS (от root):
curl -fsSL https://raw.githubusercontent.com/Lexus2016/claude-code-studio/main/install/setup-linux.sh | bash

# macOS:
curl -fsSL https://raw.githubusercontent.com/Lexus2016/claude-code-studio/main/install/setup-macos.sh | bash

# Windows (PowerShell от администратора):
irm https://raw.githubusercontent.com/Lexus2016/claude-code-studio/main/install/setup-windows.ps1 | iex
```

Каждый скрипт устанавливает Node.js, Claude Code CLI, настраивает авторизацию и создает workspace. Детали в [`install/README.md`](install/README.md).

**Шаги:**

1. **Добавьте SSH-хост** (один раз на сервер)
   - Левая панель → ⚙️ Настройки → **Удаленные хосты**
   - Нажмите **+ Добавить хост** → введите имя хоста, порт, SSH-ключ
   - Нажмите **Test Connection** для проверки

2. **Создайте Удаленный проект**
   - Левая панель → 🗂 Проекты → **+ Новый проект**
   - Тип **Remote** → выберите SSH-хост
   - Введите путь к директории на сервере
   - Нажмите **Создать**

3. **Работайте нормально**
   - Чат, Kanban, файловый браузер — всё выполняется на сервере
   - Сессии и история сохраняются локально в Claude Code Studio

### Пример: 5 production-серверов

```
Цель: обновить nginx на 5 серверах и проверить.

1. Добавьте 5 хостов: prod-eu-01 ... prod-eu-05
2. Создайте Удаленный проект для каждого → /etc/nginx
3. Откройте 5 Kanban-карточек параллельно:
   → «Обновите nginx до 1.27, протестируйте конфиг, перезагрузите, health-check»
4. Все 5 выполняются одновременно. Live-вывод по каждому серверу.
5. Ошибка → карточка остается в «В процессе» с деталями ошибки

Результат: 5 серверов обновлены и проверены за ~3 минуты, без ручных SSH.
```

### Пример: GPU-сервер для ML-задач

```
У вас есть мощный ML-сервер (GPU, 256GB RAM).
Claude Code Studio на вашем ноутбуке не может справиться с обучением локально.

→ Добавьте удаленный хост: ml-server.internal
→ Создайте Удаленный проект → /home/ml/projects
→ Чат: «Скачайте датасет, препроцессируйте, тренируйте модель»
→ Claude запускается на ml-server: использует GPU, доступ к данным локально, завершает быстро
→ Вы видите live-вывод в браузере — ваш ноутбук работает нормально
```

### SSH-безопасность

- **Пароли зашифрованы:** SSH-пароли хранятся с использованием AES-256-GCM шифрования — никогда в открытом виде. Ключ шифрования находится в `data/hosts.key` (не попадает в git)
- **SSH-ключи хранятся локально:** Файлы ключей остаются на диске; используются только при подключении, не копируются и не хранятся где-либо ещё
- **Без перенаправления ключей:** Ключи используются только для установления соединения
- **Тестирование соединения:** Всегда тестируйте перед созданием проектов

---

## Защита от конфликтов (файловые блокировки)

Если запустить несколько процессов Claude Code на одном проекте одновременно, они могут попытаться редактировать один файл параллельно — и последняя запись молча перезапишет изменения другого.

Claude Code Studio решает это автоматически через **file-lock хуки**:

- Перед редактированием файла → проверяется не редактирует ли его уже другой процесс Claude
- Если да → ждёт пока файл освободится (проверка каждые 3 секунды)
- После редактирования → сразу снимает блокировку
- Сбои процессов → устаревшие блокировки обнаруживаются через PID и снимаются автоматически

```
Claude A (задача 1)         Claude B (задача 2)
──────────────────          ────────────────────────────
Edit server.js              Edit server.js  ← одновременно
→ захватывает блокировку    → ждёт (занято)
→ редактирует файл ✓         ⏳ проверка каждые 3с...
→ снимает блокировку        → свободно! захватывает блокировку
                            → редактирует файл ✓
```

Хуки устанавливаются автоматически при `npm install`. Работает на macOS, Linux и Docker.

---

## Конфигурация

### Переменные окружения (`.env`)

Скопируйте `.env.example` в `.env` и измените по необходимости:

```env
PORT=3000                    # Порт для запуска
WORKDIR=./workspace          # Где Claude работает (ваши файлы проекта)
MAX_TASK_WORKERS=5           # Макс. параллельных процессов Claude для Kanban
CLAUDE_TIMEOUT_MS=1800000    # Максимальное время выполнения задачи в мс (по умолч.: 30 минут)
TRUST_PROXY=false            # Установите true если за nginx/Caddy
LOG_LEVEL=info               # Логирование: error | warn | info | debug
```

Для SDK-движка (необязательно, если хотите использовать API-ключ вместо `claude` CLI):
```env
ANTHROPIC_API_KEY=sk-ant-...
```

### Добавление MCP-серверов
Левая панель → ⚡ MCP → **+ Добавить MCP**

### Добавление Skills
Левая панель → 🧠 Skills → **+ Upload .md**

Или добавьте `.md` файлы напрямую в папку `skills/` и перезапустите.

### Проекты
Левая панель → 🗂 Проекты → **+ Новый проект**

Каждый проект имеет собственную рабочую директорию. Сессии привязаны к проектам.

---

## Безопасность

- Пароли хешируются через bcrypt (12 rounds) — никогда не хранятся в открытом виде
- Auth-токены: 32-байтный случайный hex, срок действия 30 дней, серверное хранение
- WebSocket защищён `httpOnly` cookie
- API-ключи никогда не передаются в браузер
- **SSH-пароли зашифрованы AES-256-GCM** — ключ хранится в `data/hosts.key`, никогда не в базе данных
- Заголовки безопасности Helmet.js на всех ответах
- Rate limiting на эндпоинтах входа

---

## Разработка

```bash
npm run dev    # авторестарт при изменении файлов (node --watch)
npm start      # запуск для продакшна
```

Без инструментов сборки, без TypeScript, без конфигураций линтинга — vanilla JS фронтенд, plain Node.js бэкенд. Откройте `public/index.html` чтобы увидеть весь фронтенд.

---

## Структура проекта

```
claude-code-studio/
├── server.js           ← главный сервер (Express + WebSocket)
├── auth.js             ← вход, хеширование пароля, сессионные токены
├── claude-cli.js       ← запуск подпроцесса claude, парсинг выходного потока
├── claude-ssh.js       ← SSH удалённое выполнение (ssh2, пароли зашифрованы)
├── config.json         ← MCP-серверы + каталог навыков
├── public/
│   ├── index.html      ← весь фронтенд (один файл: HTML + CSS + JS)
│   └── auth.html       ← страница входа/настройки
├── skills/             ← .md файлы навыков для системного промпта Claude
├── data/               ← данные рантайма (в .gitignore)
│   ├── chats.db        ← SQLite база данных (сессии + сообщения)
│   └── auth.json       ← хеш пароля
└── workspace/          ← стандартная рабочая директория Claude (в .gitignore)
```

---

## Лицензия

MIT
